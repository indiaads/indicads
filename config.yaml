data: {
  train_dir: /ssd_scratch/cvit/amruth.sagar/ads_non_ads/train,
  val_dir: /ssd_scratch/cvit/amruth.sagar/ads_non_ads/val,
  test_dir: /ssd_scratch/cvit/amruth.sagar/ads_non_ads/test,
  reshape_height: 224,
  reshape_width: 224,
  augment: True
}
model: {
  batch_size: 32,
  train_bs: 8,  # the batch size will be 4 times this if augmentation is done
  valid_bs: 8,
  vit_pretrained: google/vit-base-patch16-224-in21k,
  is_vit_trainable: True,
  epochs: 30,
  vit_feature_dim: 768,
  cls_head_dropout_p: 0.5
}
lr: {
  vit_lr : 0.000001,
  cls_lr_start : 0.1,
  cls_lr_end: 0.05,
}
ckpt: {
  ckpt_frequency: 2,
  ckpt_folder: /scratch/amruth.sagar/ADS
}
wandb: {
  wandb_log: True,
  project: Indic_ADS,
  entity: indicads,
  exp_name: 6k.data.aug.ViT.1e-6.tr.Cls.head.b32.e30.stepLR.(0.1 to 0.05).drp.p=0.5,
}
